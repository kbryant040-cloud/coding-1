4 data types we learned are STRING, INTEGERS, FLOATS, and BOOLEAN.

A code repository is a digital space for storing files and folders, specifically code.

A variable is a container that stores data like it must start with letter and cannot start with a number, can be a mixd of letters and numbers But not symbols, and Cannot be a python keyword